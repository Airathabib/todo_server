import TodoList from '../TodoList/TodoList';

describe('TodoList', () => {
  it('should create todoList with empty array', () => {
    expect(TodoList).toBeTruthy();
  });
});
test('TodoList', () => {
  expect(TodoList).toBeTruthy();
});
